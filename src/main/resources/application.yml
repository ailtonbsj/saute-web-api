spring:
    datasource:
        url: jdbc:mysql://localhost:3306/saute
        username: root
        password: YOUR_PASS
    jpa:
        show-sql: true
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
